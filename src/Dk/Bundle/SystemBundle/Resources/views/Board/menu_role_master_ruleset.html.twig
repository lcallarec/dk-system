<ul class="nav navbar-nav">
    <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown">Mes systèmes de règles Dk <b class="caret"></b></a>
        <ul class="dropdown-menu">
            {% for ruleset in rulesets %}
                <li><a href="{{ path('manage_ruleset', {id: ruleset.id}) }}">{{ ruleset.name }}</a></li>
            {% endfor %}
        </ul>
    </li>
</ul>
    
